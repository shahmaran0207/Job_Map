# Job_Map

**통근 시간 기반 거주지 지도.** 직장 주소를 입력하면 도보·대중교통·자차로 실제로 닿는 범위를 지도에 그리고, 그 안의 아파트·오피스텔·원룸 시세를 보여준다.

부동산 앱은 `지역 선택 → 매물 목록 → 회사까지 얼마나 걸리는지 따로 확인` 순서다. 이 프로젝트는 순서를 뒤집는다: `직장 위치 → 통근 30분 권역 → 그 안의 거주지와 시세`. "회사에서 가까운 방"이 최우선인 사회초년생에게 지금 이걸 한눈에 보여주는 서비스가 없다.

> **방향 전환 기록 (2026-08-26)**
> 원래 구상은 반대 방향, 즉 "자취방에서 가까운 근무지"를 보여주는 **채용 지도**였다. 통근 엔진은 그대로지만 데이터 축을 바꿨다. 이유는 **데이터 접근성**이다.
>
> 채용 공고 데이터는 잡보드의 사업 그 자체여서 개방할 인센티브가 없다. 고용24 채용정보 API 는 개인회원을 런타임에 차단하고(실측 확인), 공공데이터포털 경로는 KOGL 제4유형(비상업적 이용만)이며, 잡코리아 API 는 공공기관·학교 우선이다. 즉 **승인에 의존하는 어떤 경로도 수익화를 지탱하지 못한다.**
>
> 반면 국토부 전월세 실거래가는 **이용허락범위 제한 없음(상업적 이용 가능) + 자동승인 + 전국 + 10,000회/일**이다. 정부가 개방 의무를 지는 데이터라 구조가 다르다.
>
> 채용 축은 버리지 않았다. 스키마와 수집기가 그대로 남아 있고, 승인이 오면 레이어로 얹는다. 그때 `출근지 ↔ 거주지` 양방향이 완성된다.

---

## 1. 범위

- **지역**: 전국. 시군구 단위로 훑는다.
- **주택 유형**: 아파트 / 오피스텔 / 연립다세대 / 단독다가구 전월세.
- **이동 모드**: 도보 / 대중교통(도보 접근 포함) / 자차(목적지 최근접 주차장까지 운전 + 주차장에서 목적지까지 도보).
- **필터**: 보증금, 월세, 전용면적, 주택 유형, 건축년도, 이동수단, 통근 시간.

**하지 않는 것** — 개별 매물 목록은 제공하지 않는다. 실거래가는 과거 거래 기록이지 현재 매물이 아니다. 우리가 파는 것은 "어디를 봐야 하는지"이고, 실제 매물은 네이버부동산·직방 검색 딥링크로 넘긴다. 크롤링이 0이고, 부동산 리드 수익 모델과도 같은 구조다.

---

## 2. 이 프로젝트의 자산은 무엇인가

통근 시간 필터 자체는 방어 가능한 기능이 아니다. 실제 자산은 두 개다.

1. **장소 좌표 해결 엔진** (`src/lib/place-resolver.ts`) — 공개 데이터는 좌표를 주지 않고 주소 해상도가 시군구~법정동 수준이다. 핵심 착안점은 **`고유명 + 지역` 키워드 검색**이며, 실측 결과 상세주소로 얻은 좌표와 1m 수준으로 일치한다.

   ```
   상세주소 "성남시 분당구 판교역로 166" → (127.11045, 37.39530)
   이름+지역 "카카오 경기 성남시 분당구"  → (127.11046, 37.39545)
   "래미안대치팰리스 서울 강남구"        → 서울 강남구 삼성로51길 37
   "헬리오시티 서울 송파구"              → 서울 송파구 송파대로 345
   ```

   도메인에 중립적이라 거주지·근무지에 모두 쓰인다.

2. **통근 등시선 + 시세 결합 데이터** — "통근 30분 권역별 주거비"는 어디에도 없는 축이다. 부동산 시세 데이터는 지역 단위로만 존재하고, 통근 시간과 교차된 것은 없다.

> 그래서 개발 순서는 지도가 아니라 **수집기와 좌표 엔진이 먼저**다.

---

## 3. 수익 모델

무료 공개 지도는 매출원이 아니라 (1) 유입·PR, (2) 사용자 신고로 좌표를 교정하는 검증 루프, (3) B2B 영업 시 "실제로 쓰이는 데이터"라는 증명 역할을 한다. 매출은 그 위에 얹은 데이터 레이어에서 난다.

| 축 | 고객 | 단가 | 필요 조건 |
|---|---|---|---|
| **부동산 중개 리드** — 사용자가 통근권과 예산을 확정한 직후 매물 검색으로 넘긴다. 의사결정 직전 트래픽이라 전환이 높다 | 중개 플랫폼, 중개사 | 건당 수만~수십만 | 트래픽. **가장 빠른 현금** |
| **통근권별 주거비 지수** — "판교 통근 30분 내 원룸 중위 월세" 같은 지표는 어디에도 없다 | 기업 복지·보상팀, 언론, 리서치 | 구독 | 12개월+ 시계열 |
| **기업 사택·주거지원 컨설팅** — 사옥 이전 시 "직원들이 어디에 살 수 있는가"는 정량 답이 없는 질문이다 | 대기업 HR/총무 | 건당 수백만~수천만 | 통근 격자 + 시세 |
| **상업용 부동산 / 입지 분석** — 통근 접근성은 오피스·상권 가치의 핵심 변수 | 개발사, 리츠, 프랜차이즈 | 구독 | 전국 커버리지 |
| **공공·지자체** — 청년 주거 정책, 통근 사각지대, 교통 투자 효과 측정 | 지자체, 공공기관 | 용역 수천만 | 전국 데이터 |
| **이사·인터넷·가전 리드** | 이사업체, 통신사 | 리드 CPA | 트래픽 |
| *(채용 축 복원 시)* 공고 접근성 진단, HR 인재풀 리포트, 채용 얼터너티브 데이터 | — | — | 채용 데이터 접근권 |

도달 순서: **부동산 리드(트래픽 확보 직후) → 주거비 지수 + 사택 컨설팅(6~12개월) → 입지 분석·공공 용역(12개월+)**.

핵심 근거: 사용자는 통근 시간과 예산으로 지역을 좁힌 **직후에** 매물을 찾는다. 그 시점의 트래픽은 부동산 업계에서 가장 값이 높다. 우리는 매물을 갖지 않고도 그 트래픽을 만든다.

---

## 4. 기술 선택과 이유

### 4.1 언어: TypeScript (프론트·백엔드 공통)

`TypeScript`는 언어, `React`는 UI 라이브러리, `Next.js`는 React 프레임워크다. 서로 대체재가 아니라 층이 다르다. 이 프로젝트의 결정은 **"Next.js(React)를 쓰고, 그것을 JavaScript가 아니라 TypeScript로 쓴다"** 이다.

**왜 JavaScript가 아니라 TypeScript인가**
이 프로젝트는 UI 앱이 아니라 데이터 파이프라인이다. 공고 객체 하나가 필드 20개를 달고 `수집기 → DB → API → 지도` 4단을 지나간다. JS라면 `salaryMin`을 어딘가에서 `salary_min`으로 오타 냈을 때 예외가 나지 않고 조용히 `undefined`가 적재되며, **그 사실이 몇 달 뒤에 발견된다.** 이 프로젝트의 자산은 되돌릴 수 없는 시계열이므로 조용한 데이터 오염이 최악의 실패 모드다. `NormalizedPosting` 타입 하나가 수집기가 5개로 늘어도 적재 코드를 하나로 유지시켜 준다.

**왜 프론트·백엔드를 같은 언어로 묶는가**
수집기가 만든 타입을 API가 소비하고 프론트가 렌더한다. 여기서 언어가 갈리면 스키마 드리프트가 반드시 생기고, 그 드리프트가 곧 데이터 오염이다.

**탈락한 백엔드 후보**

| 후보 | 탈락 이유 |
|---|---|
| Python | 강점인 pandas/geopandas가 필요한 지점이 없다 — 공간 연산은 PostGIS가 담당한다. 개발 환경에 미설치 상태이기도 하다 |
| Java/Kotlin + Spring | 견고하지만 램 소비가 크고 서버리스 무료 배포에 맞지 않는다. **월 0원 운영 전략과 정면 충돌** |
| Go | 성능 이득이 실제 병목(라우팅 엔진, DB)에 없고 프론트와 타입 공유가 불가능하다 |

> 예외: 알터너티브 데이터용 통계 분석(시계열 회귀, 실적 상관 검증) 단계에서는 Python을 추가한다. 같은 Postgres를 읽기만 하므로 결합도가 0이다. 지금 넣을 이유는 없다.

### 4.2 데이터베이스: PostgreSQL + PostGIS

이 프로젝트의 연산은 대부분 공간 연산(등시선 폴리곤 안의 공고 찾기, 근무지 최근접 주차장 찾기, 격자 집계)이다. 애플리케이션 코드가 아니라 **PostGIS 안에서 SQL로** 처리하는 것이 몇 배 빠르고 코드도 적다. `ST_Contains`, `ST_DWithin`, GiST 인덱스가 핵심이다.

### 4.3 지도: MapLibre GL JS (Kakao Maps SDK 아님)

성능 때문이다. 전국 공고 수만 건을 지도에 뿌려야 하는데 Kakao SDK 마커는 수천 개에서 프레임이 무너진다. MapLibre GL은 WebGL 기반이라 클러스터링으로 10만 포인트를 처리하고, 등시선 폴리곤 오버레이도 정석적으로 지원한다.

- 배경 타일: VWorld(국토부, 무료) 또는 OSM
- **주소·상호 검색만 Kakao Local REST API** — 지도 SDK 없이 REST만 사용. 한국 주소/상호 검색은 대안이 없다
- 포인트가 더 늘면 deck.gl 레이어 추가

### 4.4 라우팅 엔진: 자체 호스팅 (상용 길찾기 API 아님)

카카오/네이버 길찾기 API는 정확하지만 호출당 과금이고 결과 저장에 약관 제약이 있다. 무엇보다 순진하게 만들면 `사용자 N명 × 공고 M건` 만큼 호출해야 하고, 공고 5만 건이면 사용자 한 명의 검색에 5만 콜이다.

**해결 1 — 등시선(isochrone)**: "각 공고까지 몇 분?"이 아니라 "내 위치에서 30분에 닿는 영역"을 폴리곤 하나로 계산하고, 그 안의 공고를 PostGIS 공간 쿼리로 뽑는다. 5만 콜 → 1콜.

**해결 2 — 등시선 영구 캐시**: 출발지를 격자에 스냅해 `(격자, 모드, 분)` 키로 폴리곤을 저장한다. 같은 동네의 두 번째 사용자부터는 라우팅 엔진을 아예 호출하지 않는다. 엔진이 죽어 있어도 캐시된 지역은 계속 서비스된다.

**엔진**
- 도보 / 자차: **Valhalla** + OSM
- 대중교통: **OpenTripPlanner 2** + 국토부 표준 GTFS
- 자차 경로 = `출발지 →(운전)→ 목적지 근처 주차장 후보 →(도보)→ 목적지` 2구간 합산. 후보 중 총 소요시간 최소를 채택. 주차장은 국토부 전국 주차장 정보 표준데이터 사용

### 4.5 데이터 소스

크롤링 얘기에서 출발했지만, 대형 사이트 직접 크롤링은 약관 위반이고 차단도 강하다. 무엇보다 채용공고 크롤링을 저작권법상 데이터베이스제작자 권리 침해로 인정한 판례가 있다. **수익화를 목표로 하는 순간 파이프라인 자체가 리스크가 된다.** 그래서 공식 개방 데이터만 쓴다.

**주 소스 — 국토교통부 전월세 실거래가 (공공데이터포털)**

| 항목 | 값 |
|---|---|
| 라이선스 | **이용허락범위 제한 없음 → 상업적 이용 가능** |
| 승인 | 자동승인 (개발·운영 단계 모두) |
| 트래픽 | 개발계정 10,000회/일, 운영계정 확장 가능 |
| 조회 단위 | 법정동코드 5자리(시군구) + 계약년월 6자리 |
| 커버리지 | 전국, 2021년 6월부터 |
| 최대 해상도 | 건물 단위 (동/호는 개인정보로 미제공) |

데이터셋 4종 — 단독/다가구(15126472) / 연립다세대(15126473) / 아파트(15126474) / 오피스텔(15126475). **주택 유형별로 응답 필드명이 다르다**(아파트는 `보증금액`/`월세금액`, 오피스텔은 `보증금`/`월세`). 그래서 후보 키를 순회하는 방어적 매핑을 쓰고 `raw` 에 원본을 전량 보존한다.

단독/다가구는 개인정보 보호로 **지번정보 일부만 제공**된다. 상당수가 건물 단위 좌표를 얻지 못하므로 `commute_usable=false` 로 격리하고 "이 동네 시세"로만 표시한다.

공공데이터포털은 **계정당 인증키 1개를 모든 API 에 공용**으로 쓴다(고용24와 반대). 엔드포인트 URL 이 문서에 노출되지 않아 `npm run probe:molit` 이 후보를 훑어 찾아낸다.

**보강 소스 (예정)**
- 지하철역·버스정류장 위치 — 통근 편의 지표
- 학교·편의시설 — 주거 선택 부가 정보
- 국민연금 사업장 데이터 — 채용 축 복원 시 근무지 주소 보강

**채용 축 (보류)** — 접근 경로를 모두 실측했고 결과는 이렇다.

| 소스 | 상태 |
|---|---|
| 고용24 채용정보 API | ❌ 개인회원 런타임 차단 (호스트·파라미터 5가지 변형 모두 동일) |
| 공공데이터포털 워크넷 | ⚠️ 자동승인이지만 **KOGL 제4유형 = 비상업적 이용만** |
| 사람인 오픈API | ⚠️ 심사. 근무지 해상도가 시군구까지 |
| 잡코리아 API | ❌ 공공기관·학교 우선 |
| 기업 채용페이지 JSON-LD | ✅ 승인 불필요, 상업적 제약 없음. 시드 구축 필요 |

즉 승인에 의존하는 경로는 수익화를 지탱하지 못한다. 채용 스키마와 수집기는 그대로 두고, JSON-LD 수집기가 준비되면 레이어로 얹는다.

---

## 5. 스키마 설계에서 중요한 두 가지

### 5.1 시계열은 "일별 행"이 아니라 "구간"으로

공고 5만 건 × 365일 = 1800만 행이 되어 무료 티어를 넘긴다. 대신 `posting.first_seen_on ~ last_seen_on` 구간으로 저장하고, 일별 활성 공고 수는 구간 겹침 쿼리로 복원한다(일 1회 이상 수집 전제). 내용이 바뀐 순간만 `posting_change`에 기록한다. 무손실이면서 저장량이 두 자릿수 적다.

### 5.2 수집 실패와 진짜 0을 구별해야 한다

수집기가 조용히 실패한 날을 "공고 대량 마감"으로 기록하면 시계열에 가짜 절벽이 생기고, **이것이 이 프로젝트의 유일한 회복 불가능한 손상**이다. 데이터 상품 전체의 신뢰도가 여기서 무너진다. 그래서:

- 매 실행을 `collect_run`에 기록한다 (`ok` / `partial` / `failed`)
- 소스를 **끝까지 다 읽은 실행(`complete`)에서만** 미관측 공고를 마감 처리한다
- `npm run stats`가 성공 실행이 없는 날, 즉 시계열 결측일을 항상 출력한다

---

## 6. 배포: 월 0원 구성

| 층 | 선택 | 비용 |
|---|---|---|
| DB | **Supabase** (Postgres + PostGIS) | 무료 |
| 수집기 크론 | **GitHub Actions** (하루 2회) | 무료 |
| 프론트 + API | **Vercel** (Next.js) | 무료 |
| 지도 타일 / 주소검색 | VWorld or OSM / Kakao Local API | 무료 |
| 라우팅 엔진 | Valhalla + OTP2 (Docker) | 아래 참고 |

라우팅 엔진만 무료 티어에 들어가지 않는다. 전국 OTP2는 램 8~16GB를 요구해 서버리스에 올릴 수 없다. 3단계로 간다.

1. **지금 (링크 공유 단계)** — 라우팅 엔진을 개발 PC의 Docker에 띄우고 **Cloudflare Tunnel**로 노출한다. 무료, 포트포워딩·고정IP·인증서 불필요, `https://*.trycloudflare.com` 주소가 바로 나온다. PC가 꺼져 있으면 라우팅만 멈추고 지도·공고 목록은 Vercel+Supabase에서 계속 살아 있다. 캐시된 등시선도 계속 서비스된다.
2. **상시 운영이 필요해지면** — **Oracle Cloud Always Free**: ARM Ampere 4코어 / 램 24GB / 스토리지 200GB 영구 무료. 전국 OTP2가 실제로 돌아간다. 단 서울·춘천 리전 ARM 재고가 자주 없어 인스턴스 생성에 며칠 걸릴 수 있다.
3. **그것도 막히면** — Hetzner ARM CAX21(램 8GB) 월 €6 수준. 최저 유료 마지노선.

---

## 6.5 보안

보안은 부가 기능이 아니라 설계 제약으로 다룬다. 전체 위협 모델과 방어는 **[SECURITY.md](./SECURITY.md)** 에 있다. 요약하면 이 프로젝트의 위험 지점은 세 곳이다.

1. **라우팅 엔진 무인증 노출** (가장 위험) — Valhalla/OTP2에는 인증 기능이 없다. 개발 PC를 터널로 노출하는 배포 계획상, URL이 유출되면 누구나 당신 PC의 CPU를 쓸 수 있다. Cloudflare Access + nginx 인증 프록시 + 엔진 포트 미노출 3중 계층으로 막는다 (`docker/`)
2. **SSRF** — 2차 수집기가 임의 기업 URL로 요청을 보낸다. 클라우드 메타데이터·내부망 접근 경로다. `safeFetch()` 를 유일한 외부 요청 경로로 두고 호스트 allowlist, DNS 사설대역 검사, 리다이렉트 차단을 적용한다
3. **사용자 위치 = 민감 개인정보** — 출발지는 사실상 집 주소다. 격자 스냅 후에만 저장·캐시·로깅한다 (`src/lib/grid.ts`)

절대 규칙 8개(TLS 검증 우회 금지, SQL 문자열 보간 금지, `dangerouslySetInnerHTML` 금지 등)는 SECURITY.md 0장에 있다.

## 7. 실행 방법

```bash
npm install
cp .env.example .env      # DATABASE_URL, WORKNET_API_KEY, KAKAO_REST_KEY 채우기

npm run db:check          # 연결 / TLS / PostGIS 진단 — 문제 원인을 특정해준다
npm run migrate           # 스키마 생성 (PostGIS 확장 포함)
npm run collect           # 공고 수집 + 적재
npm run geocode           # 근무지 좌표 확보 (배치, 쿼터 분리)
npm run geocode -- --improve   # 통근 계산 불가 근무지만 재시도 (정밀도 개선)
npm run stats             # 데이터 규모 / 좌표 품질 / 시계열 결측일 점검

npm run dev               # 지도 UI (http://127.0.0.1:3000)
npm start                 # 위와 동일 (dev 서버)
npm run build && npm run start:prod   # 프로덕션 빌드로 실행

npm run test:parse        # 응답 파싱 회귀 테스트 (네트워크·DB 불필요)
npm run test:molit        # 실거래가 파싱·금액 단위 회귀 테스트
npm run test:ingest       # 적재 경로 스모크 테스트 (실제 DB, 끝나면 정리)
npm run test:resolver     # 근무지 좌표 해결기 (실제 Kakao API 호출)
npm run typecheck
```

`db:check`를 먼저 돌리세요. 연결 실패 원인(예시 값 미교체, IPv6 전용 direct 연결, 비밀번호 percent-encoding 누락, 자체 CA 인증서 필요, pooler 사용자명 형식)을 구분해서 알려줍니다.

`test:ingest`는 배치 적재 SQL을 실제 DB에서 검증합니다. 타입 검사로는 `jsonb_to_recordset` 컬럼 정의, 표현식 유니크 인덱스에 대한 `ON CONFLICT`, 시계열 구간 연장, 날짜 시간대 왕복이 맞는지 알 수 없습니다. 적재 로직을 건드리면 이걸 돌리세요.

첫 실행 시 `COLLECT_MAX_PAGES=2` 로 시작해 워크넷 API 필드 매핑을 확인한 뒤 전량 수집으로 넘어갈 것을 권한다. 매핑이 틀려도 `posting.raw`에 원본 payload가 전량 보존되므로 나중에 무손실로 재처리할 수 있다.

### 필요한 API 키

- **고용24 오픈API**: https://openapi.work.go.kr — 회원가입 후 인증키 신청 (무료)
- **Kakao Developers**: https://developers.kakao.com — 애플리케이션 생성 후 REST API 키 (Local API 무료 쿼터)
- **Supabase**: https://supabase.com — 프로젝트 생성 후 Database URI. SQL Editor에서 `create extension postgis;` 가 되는지 먼저 확인

---

## 8. 디렉터리

```
SECURITY.md                  위협 모델 + 절대 규칙
db/001_init.sql              스키마 (설계 의도가 주석에 남아 있다)
src/lib/env.ts               환경변수 + KST 기준일 + 로그 마스킹
src/lib/db.ts                Postgres 풀 / TLS 검증 / 트랜잭션
src/lib/security.ts          SSRF 방어, safeFetch, 정제, 프로토타입 오염 차단
src/lib/grid.ts              격자 스냅 (등시선 캐시 키 + 위치 개인정보 보호)
src/lib/routing.ts           라우팅 엔진 클라이언트 (인증 프록시 경유)
src/lib/text.ts              상호 정규화, 급여·날짜·경력 파싱, 외부 문자열 관문
src/lib/geocode.ts           Kakao 지오코딩 + 영구 캐시 + 정밀도 산정
src/collectors/types.ts      NormalizedPosting — 모든 수집기의 공통 출력 계약
src/collectors/worknet.ts    고용24 오픈API 수집기
src/collectors/ingest.ts     적재 + 변경 감지 + 마감 처리
src/collectors/run.ts        오케스트레이터 + collect_run 기록
src/scripts/                 migrate / geocode-pending / stats
docker/                      Valhalla + OTP2 + nginx 인증 프록시
.github/workflows/collect.yml   무료 크론
.github/workflows/security.yml  npm audit / gitleaks / CodeQL
```

## 9. 다음 작업

상세 계획은 [TODO.md](./TODO.md), 결정과 그 근거는 [docs/JOURNAL.md](./docs/JOURNAL.md).

- [ ] 실거래가 수집기 (`npm run probe:molit` 으로 엔드포인트·필드 확정 후)
- [ ] 법정동코드 마스터 적재 + 수집 우선순위 (10,000회/일 한도 안에서 전국 훑기)
- [ ] 건물 좌표 해결 배치 (`place-resolver` 재사용)
- [ ] Valhalla / OTP2 Docker Compose + 등시선 API
- [ ] 전국 주차장 데이터 적재 + 자차 2구간 경로
- [x] Next.js + MapLibre 지도 — 1단계 완료 (직선 반경). 등시선 교체는 라우팅 엔진 이후
- [ ] 매물 검색 딥링크 (네이버부동산·직방)
- [ ] Cloudflare Tunnel 배포
- [ ] *(승인 시)* 채용 레이어 복원
