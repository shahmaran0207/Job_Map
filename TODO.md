# TODO

작업 순서는 위에서 아래로. 각 항목의 "왜"는 [README](./README.md), 보안 제약은 [SECURITY.md](./SECURITY.md) 참고.

---

## 0. 지금 막혀 있는 것

### 워크넷 채용정보 API 키 확보 ← 최우선

현재 `WORKNET_API_KEY`(고용24 발급 UUID)로는 채용정보를 못 받는다. `npm run keys:check` 결과:

```
[work24] www.work24.go.kr    ✗ 개인회원은 사용할 수 없는 OPEN-API입니다.
[legacy] openapi.work.go.kr  ✗ 유효하지 않은 인증키 입니다.
```

**고용24의 채용정보 API는 기업·기관회원 전용**이다. 개인 계정으로는 어떤 엔드포인트로도 안 된다.

대안 두 가지:

| 경로 | 접근성 | 제약 |
|---|---|---|
| **공공데이터포털** `한국고용정보원_워크넷 채용정보` (data.go.kr/data/3038225) | 개인 가능, **자동승인**, 무료 | **KOGL 제4유형 — 비상업적 이용만, 변형 금지** |
| 고용24 기업회원 전환 | 사업자등록증 필요 | 상업적 이용 가능 |

→ 지금은 data.go.kr로 신청해 진행한다. 키를 받으면 `.env`에 `WORKNET_API_FLAVOR=legacy` 와 함께 넣고 `npm run keys:check` 로 확인. 코드는 이미 양쪽 엔드포인트를 지원하고 자동 판별한다.

**수익화 시점에는 이 소스를 그대로 쓸 수 없다.** 비상업 라이선스이므로, 상업화하려면 기업회원 전환 또는 한국고용정보원과 별도 협약이 필요하다. 이 사실이 아래 JSON-LD 수집기의 중요도를 크게 올린다.

### 워크넷 필드 매핑 교정

키가 통하면 `keys:check` 가 실제 응답 필드 목록을 출력한다. `worknet.ts` 의 `pick()` 후보 키를 실제 필드명에 맞게 정리한다. 매핑이 틀려도 `posting.raw` 에 원본이 전량 남으므로 무손실 재처리가 가능하다.

---

## 1. 사람인 데이터 확보 ← 커버리지의 핵심

20~30대가 실제로 보는 공고 대부분이 사람인에 올라온다. 워크넷만으로는 중소기업·공공 편중이라 데이터가 반쪽이다. **이 프로젝트의 실사용 가치가 여기 달려 있다.**

> 결론: **공식 오픈API를 쓴다.** API가 제공되는데 스크래핑할 이유가 없고, 법적 리스크를 파이프라인에 심을 이유도 없다.

### 1-A. 사람인 공식 오픈API — 확정된 방식

- 신청: https://oapi.saramin.co.kr — 이용신청 → 승인 → access-key 발급
- 제약: **일 500회 호출**. 호출당 반환 건수에 따라 전국 커버가 며칠 걸리는지 먼저 계산한다.
  전량 재수집이 불가능하면 **증분 수집 전략**이 필요하다(등록일 기준 최신순으로 훑고,
  이미 본 공고는 `last_seen_on` 만 갱신). 지금 스키마가 이미 이 방식을 지원한다
- 확인해야 할 것:
  - 응답의 **근무지 주소 해상도** (시군구까지인지 상세주소인지). 통근 계산 정밀도를 직접 결정한다
  - 상세 본문(자격요건·복리후생 서술)이 포함되는지
  - 정렬·필터 파라미터로 증분 수집이 가능한지
- `NormalizedPosting` 계약에 맞춰 `src/collectors/saramin.ts` 를 추가하면 적재 코드는 그대로 재사용된다

### 1-B. 페이지 스크래핑 — 하지 않음

검토했으나 채택하지 않는다. 이유:

- **공식 API가 존재하므로 스크래핑할 이유가 없다.** 같은 데이터를 안정적이고 정형화된 형태로 받을 수 있다
- 법적 리스크가 실재한다: 잡코리아가 사람인을 상대로 낸 소송에서 법원은 경쟁 취업사이트의
  채용공고 크롤링을 **저작권법상 데이터베이스제작자 권리 침해**로 보고 손해배상을 인정했다.
  이 프로젝트가 하려던 것과 거의 동일한 행위이며 그 사건의 피고가 사람인이었다
- 수익화를 목표로 하는 프로젝트에서 파이프라인 자체가 리스크가 되는 구조는 피한다

정형 필드가 부족한 문제는 스크래핑이 아니라 **JSON-LD 수집기(2번)** 와 **근무지 주소 정규화(3번)** 로 해결한다.

### 1-C. 이미지 내 정보 추출 (OCR) — 조건부

공고 정보가 이미지로만 제공되는 경우가 있다(근무지, 급여, 자격요건). 텍스트 파싱만으로는 놓친다.

- **선행 조건**: API 응답에 이미지 URL이 정당하게 포함되는 경우에만 진행한다
- 후보: Tesseract(무료, 한국어 정확도 낮음) / 클라우드 OCR(유료) / 멀티모달 LLM(정확도 높고 구조화 추출까지 한 번에, 건당 과금)
- 적용 범위: **정형 필드로 근무지를 얻지 못한 공고에만** 선택적으로 적용해 비용을 통제한다.
  전량에 돌리면 비용이 선형으로 늘고 얻는 게 적다

---

## 2. JSON-LD 수집기 ← 상업적으로 유효한 유일한 소스

기업 자체 채용페이지에 Google for Jobs 용으로 심어둔 `schema.org/JobPosting` 구조화 데이터를 수집한다.

- 기계 판독용으로 **공개된** 데이터라 법적 위치가 가장 안전하다
- `jobLocation` 에 주소가 구조화되어 있어 근무지 품질이 좋다
- KOGL 제약이 없으므로 **수익화 단계의 주력 소스**가 된다

주의: 임의 도메인으로 요청을 보내게 되므로 SSRF 방어가 실제 시험대에 오른다. `safeFetch()` 의 `allowHosts` 없는 경로(DNS 사설대역 검사 + 리다이렉트 차단)를 처음으로 쓰게 된다. SECURITY.md 1장의 "남은 위험(TOCTOU)" 항목대로, 자격증명이 없는 격리 환경에서 실행하는 것을 전제로 한다.

---

## 3. 근무지 주소 정규화 ← 프로젝트의 진짜 자산

- **국민연금 사업장 데이터** (공공데이터포털) 적재 후 `company.name_norm` 으로 조인. 사업장별 실제 주소 + 가입자 수 + 업종을 얻어 본사/지사를 구별한다
- 웹 검색 + LLM 으로 `회사명 + 지역` → 지사 주소 추출, 위 데이터로 교차 검증
- `worksite.geo_precision` 이 `sigungu` 이하인 건을 우선 개선한다. 통근시간 표시 가능 여부를 이 값이 결정한다

---

## 4. 라우팅 엔진

- `docker/docker-compose.yml` 로 Valhalla + OTP2 + nginx 인증 프록시 기동 (Docker Desktop 필요)
- OSM 한국 추출본 → `data/osm/`, 국토부 표준 GTFS → `data/otp/`
- 등시선 API + `isochrone_cache` 연결. `src/lib/routing.ts` 는 이미 준비됨
- 전국 주차장 표준데이터 적재 → 자차 2구간 경로(운전 + 주차장에서 도보)
- `ROUTING_TOKEN` 생성: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

---

## 5. 지도 (Next.js + MapLibre)

- 주소 검색(Kakao Local API는 **서버 사이드 프록시** 경유) → 등시선 오버레이 → 폴리곤 내 공고 핀
- 필터: 연봉 / 학력 / 경력 / 고용형태 / 직종 / 이동수단 / 시간
- 사용자 좌표는 `snapToCell()` 로 격자 스냅 후에만 저장·캐시·로깅 (집 주소 보호)
- 프론트에는 도메인 제한된 JavaScript 키만. REST 키 노출 금지
- CSP 및 보안 헤더 적용 (SECURITY.md 9장)

---

## 6. 배포

- Vercel(프론트) + Supabase(DB, 이미 구축됨) + GitHub Actions(수집 크론, 이미 구축됨)
- 라우팅 엔진은 개발 PC Docker + **Cloudflare Tunnel**, 앞단에 Cloudflare Access
- 상시 운영이 필요해지면 Oracle Cloud Always Free(ARM 4코어/24GB)로 이전

---

## 7. 나중에

- 공통코드 API(`WORKNET_CODE_API_KEY`)로 직종·지역 코드 마스터 적재 → 분류 일관성 확보 (알터너티브 데이터 상품의 전제)
- 강소기업 API → "강소기업만 보기" 필터
- 급여 × 지역 분포 지도 (B2C 훅 + 보상 벤치마크 상품)
- 적재 성능이 다시 문제되면 `COPY` 기반 스테이징 테이블 전략 검토
