# 저장소 안에서는 LF 로 통일한다. Windows 작업 트리와 CI(Linux) 사이에서
# 줄바꿈 차이로 diff 가 오염되는 것을 막는다.
* text=auto eol=lf

# nginx 설정과 셸 스크립트는 컨테이너(Linux)에서 실행되므로 반드시 LF.
*.inc      text eol=lf
*.template text eol=lf
*.sh       text eol=lf

*.png binary
*.jpg binary
*.pbf binary
*.zip binary
