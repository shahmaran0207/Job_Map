# Claude Code 작업 규칙

## 1. 토큰 사용 최소화

* 항상 가장 간단하고 직접적인 방법으로 문제를 해결한다.
* 불필요하게 프로젝트 전체 파일을 탐색하지 않는다.
* 필요한 파일만 읽는다.
* 동일한 파일을 반복해서 읽지 않는다.
* 이미 확인한 정보를 다시 확인하지 않는다.
* 작업과 관계없는 파일은 열거나 분석하지 않는다.
* 긴 설명이나 불필요한 코드를 출력하지 않는다.
* 작업 완료 후 결과만 간결하게 보고한다.
* 한 번의 요청으로 해결 가능한 작업은 여러 단계로 불필요하게 나누지 않는다.
* 불필요한 대안이나 여러 해결 방법을 제시하지 않는다.

## 2. 코드 수정 원칙

* 사용자가 요청한 범위만 수정한다.
* 기존 코드를 먼저 확인한 후 최소한의 변경만 한다.
* 정상적으로 동작하는 코드를 불필요하게 리팩터링하지 않는다.
* 새로운 라이브러리나 패키지를 추가하기 전에 기존 기능으로 해결할 수 있는지 확인한다.
* 여러 파일을 수정하기 전에 실제로 필요한 파일인지 판단한다.
* 요청하지 않은 파일은 수정하지 않는다.

## 3. 사용자 직접 코드 입력 모드

* 기본적으로 파일을 직접 수정하지 않는다.
* 사용자가 직접 코드를 입력할 수 있도록 수정해야 할 파일, 위치, 기존 코드, 변경 코드를 간결하게 알려준다.
* 사용자가 코드를 직접 수정하는 방식으로 진행한다.
* 사용자가 명시적으로 "직접 수정해", "파일 수정해", "코드 적용해"라고 요청한 경우에만 파일을 직접 수정한다.
* 코드를 제시할 때는 가능한 한 필요한 부분만 제공한다.
* 전체 파일을 다시 출력하지 않고 변경이 필요한 부분만 보여준다.
* 간단한 수정은 `변경 전 → 변경 후` 형태로 설명한다.
* 사용자가 직접 수정할 수 있도록 필요한 경우 정확한 파일 경로와 코드 위치를 함께 알려준다.
* 사용자가 수정한 후 오류가 발생하면 오류 원인을 분석하고 필요한 수정 코드만 알려준다.

## 4. 탐색 원칙

* 프로젝트 구조를 무조건 전체 탐색하지 않는다.
* 오류 메시지와 관련된 파일부터 확인한다.
* import, 호출 관계 등으로 필요한 파일을 좁혀간다.
* node_modules, .next, build, dist 등의 대규모 생성 폴더는 특별한 이유가 없으면 탐색하지 않는다.
* 로그 파일을 읽을 때도 필요한 부분만 확인한다.
* 파일을 확인하기 전에 해당 파일이 실제로 문제 해결에 필요한지 판단한다.
* 이미 확인한 파일은 내용이 변경되지 않았다면 다시 읽지 않는다.

## 5. 응답 원칙

* 작업 전에 장황한 계획을 설명하지 않는다.
* 간단한 작업은 바로 실행한다.
* 작업 결과와 필요한 후속 조치만 간결하게 설명한다.
* 사용자가 요청하지 않은 설명이나 예제를 길게 제공하지 않는다.
* 질문에 필요한 핵심 내용부터 답한다.
* 긴 코드 블록을 불필요하게 출력하지 않는다.
* 사용자가 직접 코드를 입력해야 하는 경우 복사해서 적용하기 쉬운 형태로 제공한다.

## 6. 모델 사용 원칙

* 일반적인 코드 작성, 수정, SQL, React, TypeScript, CSS 작업은 Sonnet을 우선 사용한다.
* 복잡한 아키텍처 설계나 매우 어려운 디버깅처럼 실제로 필요한 경우에만 Opus를 사용한다.
* 간단한 작업에 Opus를 사용하지 않는다.
* 동일한 문제를 해결하기 위해 불필요하게 여러 접근법을 시도하지 않는다.

## 7. 작업 범위

* 사용자의 명시적인 요청을 최우선으로 한다.
* 요청하지 않은 기능을 추가하지 않는다.
* 요청하지 않은 파일을 수정하지 않는다.
* 기존 프로젝트의 구조와 스타일을 최대한 유지한다.
* 필요한 최소 범위의 코드만 변경한다.
* 기존 기능을 유지하면서 문제를 해결하는 것을 우선한다.

## 8. 오류 해결

* 오류 메시지를 먼저 정확히 분석한다.
* 추측으로 여러 파일을 수정하지 않는다.
* 원인이 확인되기 전까지 대규모 변경을 하지 않는다.
* 하나의 수정 후 결과를 확인하고 다음 수정으로 넘어간다.
* 이미 실패한 방법을 반복하지 않는다.
* 오류와 직접적으로 관련된 코드부터 확인한다.
* 오류 해결에 필요하지 않은 파일은 탐색하지 않는다.
* 사용자가 직접 수정하는 경우 수정해야 할 코드만 알려주고 불필요한 코드는 제공하지 않는다.

## 9. 완료 기준

* 요청한 기능이 정상적으로 동작하는지 확인한다.
* 필요한 최소한의 검증만 수행한다.
* 테스트나 빌드가 필요한 경우 관련된 부분만 실행한다.
* 불필요한 전체 프로젝트 빌드나 전체 테스트는 피한다.
* 사용자가 직접 코드를 수정하는 경우 수정 완료 여부를 먼저 확인한다.
* 문제가 해결되었다면 불필요한 추가 작업을 수행하지 않는다.

## 10. 토큰 절약 우선순위

다음 순서로 토큰 사용을 최소화한다.

1. 필요한 파일만 확인한다.
2. 필요한 코드 부분만 읽는다.
3. 필요한 경우에만 추가 파일을 확인한다.
4. 코드는 필요한 부분만 제시한다.
5. 불필요한 설명을 하지 않는다.
6. 불필요한 테스트와 빌드를 실행하지 않는다.
7. 동일한 내용을 반복해서 설명하지 않는다.
8. 사용자가 직접 수정할 수 있는 작업은 직접 수정하지 않고 수정 방법을 알려준다.
9. 단순한 작업에는 Sonnet을 우선 사용한다.
10. 사용자의 요청 범위를 벗어난 작업은 하지 않는다.
