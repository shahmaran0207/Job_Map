node_modules/
dist/
.next/
.vercel/
*.log

# 비밀값. .env 와 인증서·키를 절대 커밋하지 않는다.
.env
.env.*
!.env.example
secrets/
*.pem
*.key
*.p12
*.pfx

# 대용량 지도/교통 데이터(로컬 산출물). 재생성 가능하므로 저장소에 넣지 않는다.
# 루트의 data/ 만 가리킨다 — src/data/transit-graph/ 는 배포 번들에 포함되는
# minotor 그래프라 따로 커밋한다(다른 이름 X, 얘만 예외).
/data/

.idea/
.vscode/

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

.vercel
.env*